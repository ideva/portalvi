<%= form_for(@dokter) do |f| %>
  <% if @dokter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dokter.errors.count, "error") %> prohibited this dokter from being saved:</h2>

      <ul>
      <% @dokter.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%#*<div class="field">%>
    <%#= f.label :kode, :class => "required" %><br />
    <%#= f.text_field :kode, :class => "required" %>
  <%#*</div>%>
  <div class="field">
    <%= f.label :nama, :class => "required" %><br />
    <%= f.text_field :nama, :class => "required" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
