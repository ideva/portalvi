<%= form_for(@laporan) do |f| %>
  <% if @laporan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@laporan.errors.count, "error") %> prohibited this laporan from being saved:</h2>

      <ul>
      <% @laporan.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
